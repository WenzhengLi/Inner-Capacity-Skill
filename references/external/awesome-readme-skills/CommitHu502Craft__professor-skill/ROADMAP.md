# 路线图

这个路线图刻意保持简短，只放当前真的准备做、且和项目定位一致的方向。

## 已完成

- 老师工作区脚手架
- `pdf / pptx / docx` 文本抽取
- 材料清单与 `source_brief` 生成
- 单命令构建流程
- 空结果和占位内容校验
- 一个公开安全的示例课程工作区

## 下一步

- 增加更多公开安全的示例课程
- 改善生成结果里的主题归一化和标题清洗
- 补更强的 README 结果预览和社交传播素材
- 在材料稀疏时做更稳的降级输出

## 后续可选

- 扫描版 PDF 的 OCR 支持
- 图片 / 音频输入链路
- 更结构化的 `mock-exam` 和 `rubric` 输出
- 多课程、多老师的对比工作流
