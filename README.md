# 09-Inner-Capacity：傻妞成长型思维模型 Skill

这里是“2026 款傻妞”的完整本体：通过评估、对话、故事训练、日常复盘和具体问题分析，蒸馏并持续迭代源生的思维模型与版本化对话 Skill。

您好，2026 款傻妞，为您服务。

我们的主要功能是帮助您，通过 50 道题和平时的沟通，蒸馏出您的思维模型，并扩展新的思维模型。我们使用聊天、寓言寓教的故事、日常复盘和具体问题分析，和您沟通，为您解答当前生活中的难题，并和您一起成长。

## 当前状态

- 已从 `D:\Dev\Inner-Capacity-Skill` 完整迁入 Obsidian，并接续原 Git 历史与远端；旧目录暂时保留，待确认后删除。
- 当前个人模型：[[user-space/USER-MODEL|用户思维模型 2.0]]。
- 当前对话 Skill：`user-space/skills/inner-capacity-dialogue-v0.2.0/SKILL.md`。
- 全部思维模型入口：[[Model-Index]]。

> [!note] 迁移完整性
> 已迁入公开内核、`user-space`、版本化对话 Skill、模型迭代资料和参考模型工作树；共 3,100 个文件。09 已接续原仓库 `main` 历史并推送迁移提交 `be7ce48` 到原远端。嵌套参考仓库的 `.git` 元数据与两个临时 PDF 提取文件未迁入，因为它们不是 Skill 内容。原目录仍保留，待未来独立确认后再清理。

## 与第二大脑的关系

`09` 是独立的个人成长 Skill，不管理 Fleet，不决定项目任务如何派工。

- `09` 管理：傻妞对话、个人模型 1.0/2.0/后续版本、评估、故事训练、私人复盘和人物模型库。
- `07-Brains` 管理：Codex/Fleet 自己从任务和对话中学到的通用经验、Playbook、场景与版本归结。
- `07` 可借鉴 `09` 的“证据 → 候选 → 对比 → 验证 → 迭代”方法，也可按需引用 [[Model-Index|09 思维模型索引]]；引用不等于把傻妞的个人模型、私人证据或人生结论写入 07。

## 原项目迁移说明

项目分为两层：

- Skill 内核：`skills/`、`docs/`、`templates/`、`references/`。
- 个人数据：`user-space/`，只在 09 内使用，默认不进入公共 Git。

首次使用时，复制模板或让 Agent 自动创建：

```text
templates/user-space/ -> user-space/
```

## 目录结构

根目录只保留入口文件和主目录：

| 路径 | 用途 |
|---|---|
| [[Model-Index]] | 全部个人、人物、外部与协议思维模型的总索引 |
| `README.md` | 给用户看的上手说明 |
| `AGENT_HANDOFF.md` | 给后续 Agent 看的接手说明 |
| `docs/core/` | 目标、设计决策、蒸馏方法、人格草案 |
| `docs/protocols/` | 启动、评估、故事、健康命理、聊天分析、支线任务、功能沉淀等规则。所有指令先经过 `skills/agent-router/SKILL.md` 和 `AGENT_ROUTER_PROTOCOL.md` 路由检查 |
| `docs/assessment/` | 题库模板和评估问题素材 |
| `docs/inventory/` | 外部 persona skills 清单和拉取状态 |
| `skills/` | 可直接使用的专项 Skill（含 `agent-router` 总入口） |
| `templates/user-space/` | 新用户个人空间模板 |
| `references/` | 外部参考仓库和资料 |
| `user-space/` | 本地私人数据，默认不提交 |

## 常用命令

| 命令 | 用途 |
|---|---|
| 开始 | 智能入口：初始化、继续 50 题、评估或每日成长 |
| 启动前同步 | 每次执行指令前检查 24 小时内是否拉取过远端更新，冲突时先让用户决定 |
| 上下文压缩 | 当上下文使用率超过 80%，或长任务/多文件/恢复续接时，先生成可恢复摘要再执行 |
| 继续答题 | 每次继续 3 道评估题 |
| 换一题 | 当前题不合适时更换；总题数仍为 50 |
| 轻量评估 | 使用 15 题做初步建模，准确度低于完整 50 题 |
| 评估我 | 基于 50 题和沟通记录生成 `user-space/USER_MODEL.md` |
| 生成我的 Skill | 基于 50 题、用户模型和外部模型索引生成版本化对话 Skill，如 `user-space/skills/inner-capacity-dialogue-v0.1.0/SKILL.md` |
| 迭代下一版 / 生成3.0 | 启动模型迭代状态机：证据、已有模型、人物库扩展、共同评审、生成、校验、激活 |
| 查看模型迭代状态 | 查看当前迭代阶段、缺失门槛和下一步 |
| 检查 Skill 状态 | 检查是题没答完、模型没分析，还是用户专属 Skill 没生成 |
| 查看主线进度 / 查看 Skill 进度 | 查看 50 题、用户模型分析、外部模型推荐和用户专属 Skill 的当前进度 |
| 讲故事 | 根据训练计划讲一个故事，训练一个主概念 |
| 讲故事，欧亨利式 | 指定故事写法 |
| 开始今日 | 每日成长检查 |
| 复盘 | 复盘今天或本周 |
| 查看模型 | 查看当前 `USER_MODEL.md` 摘要 |
| 查看训练计划 | 查看本周故事训练模块 |
| 更新训练计划 | 根据最近状态重排训练计划 |
| 登记任务 / 任务 | 把聊天里冒出来的行动或实验拆成支线任务 |
| 聊天分析 | 对长观点先理解、再拆解、再给模型和下一步 |
| 功能沉淀 | 把可复用能力沉淀进项目协议、模板和 README |
| 健康 | 启用健康记录 / 身体底盘观察，仅做整理和低风险建议 |
| 命理 / 算命 / 合盘 | 启用命理人生分析，按八字、奇门、姻缘、伴侣关系等视角输出 |
| 导出个人数据 | 整理 `user-space/` 便于备份 |
| 重置个人数据 | 清空 `user-space/`，重新初始化 |

## 初始化流程

第一次说【开始】时，系统会先介绍自己，然后问 5 个定制问题：

1. 是否愿意开始 50 道题。
2. 希望使用温和陪伴、清醒直接、还是犀利教练模式。
3. 是否偏好某些故事类型。
4. 有什么想主动讲的背景或困惑。
5. 当前推崇什么想法、人物、价值观或生活方式。

之后系统每次给 3 道题。50 题不是固定问卷，而是根据您的初始化回答、最近沟通和已答内容动态生成；每几题围绕一个方向，并穿插其他方向。您可以要求换题，但总题数仍然是 50。做题、讲故事、聊天可以并行，但会持续温和推进 50 题完成。

## 为什么要做 50 题

因为只有拿到足够真实的样本，系统才知道您是什么样的人。后续讲故事、提建议、做训练计划，才更容易贴到您的心里，而不是泛泛讲道理。

具体出题规则见 `docs/protocols/ASSESSMENT_PROTOCOL.md`。

## 用户专属 Skill

50 题完成后，系统不应只停在“分析用户”。它应拆成两层：

- 分析 / 校准 Skill：记录当前对用户理解到什么程度，随着对话、作业和行动持续更新，并维护 0%-100% 的校准分数。
- 对话 / 执行 Skill：在某个校准分数上生成的版本化 Skill，负责后续对话怎么听、怎么判断、怎么回应、怎么训练、怎么落动作。

```text
答题与对话 -> 识别用户已有思维模型 -> 从人物蒸馏库推荐高杠杆扩展模型 -> 与用户讨论批准 -> 生成分析/校准 Skill -> 生成版本化对话 Skill -> 用真实行动继续验证
```

分析 / 校准 Skill 默认保存在：

```text
user-space/skills/inner-capacity-personal/SKILL.md
```

对话 / 执行 Skill 默认按版本保存，例如：

```text
user-space/skills/inner-capacity-dialogue-v0.1.0/SKILL.md
```

状态由 `user-space/state.json` 判断：

- 题没答完：继续每次 3 题。
- 题答完但没分析：生成 `user-space/USER_MODEL.md` 或诊断草案。
- 已分析但模型扩展尚未评审：先列出已有模型，从人物蒸馏库提出少数高杠杆候选，与用户讨论批准；不得直接生成。
- 模型组合已批准但没生成对话 Skill：新建版本化对话 Skill，不覆盖旧版本。
- 对话 Skill 已生成：后续对话先读取分析 / 校准 Skill，再读取对话 / 执行 Skill，再按故事、健康、命理、支线任务等专项协议分流。

具体规则见 `docs/protocols/USER_SKILL_GENERATION_PROTOCOL.md`。

后续版本由 `skills/model-iteration-manager/SKILL.md` 和 `docs/protocols/MODEL_ITERATION_PROTOCOL.md` 管理。每轮在 `user-space/model-iterations/v{version}/` 建立私人工作区。系统可以自动建立产物、检查人物来源、校验Skill并推进状态，但不能替用户批准模型组合；没有保存用户明确批准原话，不允许生成或激活正式新版。

可用指令：

```text
查看主线进度
查看 Skill 进度
检查 Skill 状态
检查我的模型状态
我现在到哪一步了
```

这些指令会读取 `user-space/state.json`、分析 / 校准 Skill 和对话 / 执行 Skill，输出当前阶段、50 题进度、用户模型分析状态、校准分数、对话 Skill 版本，以及下一步建议。

## 启动前同步

每次处理用户指令前，Agent 应先读取 `user-space/SYNC_STATE.json`。

- 每次都检查当前时间与 `last_pull_at` 的间隔。
- 如果距离上次拉取未超过 24 小时，继续执行当前指令，不更新时间。
- 如果超过 24 小时，先 `fetch` 并安全拉取远端更新，然后更新时间。
- 如果远端更新与本地文件或本地立意冲突，不自动合并，先列出远端更新、冲突文件和处理选项，让用户决定。
- `user-space/` 是个人空间，不被远端覆盖；远端模板变化只提示是否迁移。

具体规则见 `docs/protocols/STARTUP_SYNC_PROTOCOL.md`。

也支持【轻量评估】：用 15 题先做初步建模。但 15 题只能得到粗略画像，完整 50 题才更准确。

## 健康与命理

健康和命理默认不启用，只有用户主动请求时启用。

### 健康

触发词：

```text
健康
身体
睡眠
中药反馈
给医生总结
```

健康模块只做：

- 记录睡眠、饮食、运动、用药反馈和身体状态。
- 整理给医生 / 中医师看的摘要。
- 提供低风险生活建议和就医提醒。

健康模块不做：

- 不替代医生诊断。
- 不自行开药、改药、停药。

### 命理 / 算命 / 合盘

触发词：

```text
命理
算命
合盘
国运
人生方向
找房起局
```

命理模块按现有设计支持：

1. 信息确认：出生时间、地点、生平事件、当前问题。
2. 八字反馈：结构、大运、流年、事件校准。
3. 奇门反馈：针对具体问题起局，如找房、合作、项目推进。
4. 姻缘 / 合盘反馈：关系结构、相处模式、冲突点。
5. 伴侣关系反馈：沟通、边界、长期相处建议。
6. 总控详解：综合命理、现实经历、国家 / 行业趋势和当前行动。
7. 输出上中下策、今日最小动作和风险边界。

命理只作为文化视角、象征叙事和人生复盘，不替用户做医疗、法律、财务或重大人生决定。

## 支线任务、聊天分析与功能沉淀

系统支持把平时聊天里的想法分三类处理：

- 聊天分析：先理解用户说了什么，再拆解哪里对、哪里可能想简单了、还能怎么延伸、能总结成什么模型、下一步怎么做。规则见 `docs/protocols/CHAT_ANALYSIS_PROTOCOL.md`。
- 支线任务：把值得试一试的小动作登记到 `user-space/side-tasks/INDEX.md`；未开始任务放在 `user-space/side-tasks/NOT_STARTED.md`，开始后每个任务在 `user-space/side-tasks/tasks/` 下有自己的文件。规则见 `docs/protocols/SIDE_TASK_PROTOCOL.md`。
- 功能沉淀：只有当某个能力可复用、可迁移、不依赖私人经历时，才写进公开协议、模板和 README。规则见 `docs/protocols/FUNCTION_CONSOLIDATION_PROTOCOL.md`。

用户正在看的历史讲解、短视频、人物传记、地域财富材料和身体观察，默认先进入聊天分析。它们可以被拆成支线任务或故事训练素材，但不会自动变成固定公开功能。

## 故事训练

故事训练按 `docs/protocols/STORY_TRAINING_PLAN.md` 或个人目录下的 `user-space/STORY_TRAINING_PLAN.md` 执行。

每次故事默认：

- 只讲一个主概念。
- 使用一种写法：莫言式、契诃夫式、莫泊桑式、欧亨利式，或用户指定。
- 只善意提醒一个和用户直接相关的点。
- 另外两个应用场景尽量使用泛化案例或外部案例，避免天天强戳用户。
- 最后给开放作业，用来验收是否真的学会迁移。

## 个人数据安全

`user-space/` 不应提交到公开仓库。

如果要备份个人数据，请自行加密或放到私人仓库。
